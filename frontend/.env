# Production build — Flask serves the SPA from the same origin, so leave empty.
# The browser uses window.location.origin; runtime /config.js can override via PUBLIC_API_URL.
VITE_API_BASE_URL=

# Only if the UI is hosted on a different origin than the API:
# VITE_API_BASE_URL=https://api.example.com
