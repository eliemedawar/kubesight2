apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubeconfig-pvc
  namespace: kubesight
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
